name: Auto-merge Dependabot
on:
  pull_request_target:
    types: [opened, labeled, synchronize]
permissions:
  pull-requests: write
jobs:
  auto-merge:
    uses: Aftergraph/.github/.github/workflows/auto-merge-dependabot.yml@da37fba1ccf95074f3b57c174714cff226c3379c # main
    secrets:
      automerge_pat: ${{ secrets.AUTOMERGE_PAT }}
